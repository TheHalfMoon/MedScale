# Converge notes — Spec 024

READY_BASE closed when: Strict session default + IPC server/client + writer lock exclusivity + two-client IPC deny evidence + doctor `os_ipc_qualified=true` with `multi_client_release_ready=false` + evidence LIMITATIONS honest + planning queue updated to deferred **025+**.
