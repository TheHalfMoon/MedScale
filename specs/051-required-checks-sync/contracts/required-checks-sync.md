# Contract: REQUIRED_CHECKS sync

`evidence/022-release-qualification-prep/REQUIRED_CHECKS.md` MUST contain the
literal job name:

```text
perf delivery-plan scale (windows)
```

Doctor `required_checks_packet_synced` MUST be true with `release_ready` false.
