# Plan — Spec 071

## Slice A — comparator pin and claim ledger
- Reverify the exact OpenMed v2.2.0 commit/tree and hash comparator source documents.
- Generate a 39-row ledger from the canonical parity matrix.
- Default every non-waived comparative claim to unmeasured unless a complete BenchmarkManifest is discovered.

## Slice B — runtime and capability evidence
- Bind the Spec 069 tract CPU baseline as a MedScale observation, not a winner claim.
- Record pinned OpenMed MLX/model-registry/eval/de-identification capability evidence from exact source paths.
- Distinguish feature presence from matched quality/performance parity.

## Slice C — product Evidence Center
- Replace `PROVEN ADVANTAGE` language with claim-disciplined states.
- Expose baseline, evidence references, limitations, and missing measurement in the native Evidence route.
- Keep Model Center and Evidence Center scopes distinct.

## Slice D — qualification
- Add regressions preventing unmanifested parity/surpass/superiority labels.
- Verify UI compilation, smoke/perf stability, Rust 1.88, Clippy, and workspace tests.
- Run exact-range review, exact-head CI, protected merge, and post-main verification.
