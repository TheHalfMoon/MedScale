# Clarifications — Spec 065

- CLI parity means equivalent trusted contract access where semantically useful, not pixel parity with Desktop.
- Existing `timeline`, `brief`, and `coverage` commands remain supported; grouped patient UX is additive.
- Outbox and disclosure commands are read-only in this slice.
- FHIR support output is an honest support matrix, not a live CapabilityStatement or full-conformance claim.
- Real PHI-to-stdout remains unauthorized; synthetic/permitted development scope only.
- Spec 012/MESC remains optional/deferred and untouched.
