# Analyze notes — Spec 031

- Spec ↔ plan ↔ tasks aligned on ReadyBaseMeasured Seatbelt network-deny + honest doctor axes.
- No PlatformQualified path while EXTERNAL_GATES OPEN.
- Cross-OS: Linux Landlock + Windows Job Object remain; AppContainer FS scaffold unchanged.
- Doctor honesty extended with `macos_measured` without flipping `platform_qualified`.
- Probe subprocess required (sandbox_init irreversible).
- No new crates; libSystem FFI admitted under Spec 031.
