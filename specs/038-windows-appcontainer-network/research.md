# Research — Spec 038

Zero-capability AppContainer (no InternetClient) should deny outbound TCP with WSAEACCES / access denied. TEST-NET-3 endpoint avoids relying on loopback quirks. LPAC requires additional process-creation policy and remains a separate residual.
