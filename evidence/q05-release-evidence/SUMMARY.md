# Q05 CI immutable action pins

Pinned GitHub Actions to full commit SHAs with `permissions: contents: read`.

Does **not** configure branch protection / required checks (repository-owner external gate).

Does **not** claim `RELEASE_READY`.
