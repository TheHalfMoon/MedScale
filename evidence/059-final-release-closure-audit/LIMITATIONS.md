# Spec 059 Limitations

- `RELEASE_READY = FALSE`.
- `PRIVATE_DATA_READY = FALSE`.
- No signing identity, notarization credential, app-store credential, or production secret is created or stored.
- No qualified-hardware performance attainment is claimed from GitHub-hosted runners.
- No final v0 UI or WCAG conformance is invented.
- macOS product/App Sandbox signed enforcement remains unqualified.
- Optional MESC remains untouched and non-blocking.
