# Spec 041 limitations

- Entitlements artifact + detection probe only
- Runtime App Sandbox enforcement requires signed binary (SIGNING_ACTION external)
- Seatbelt Spec 031 is not App Sandbox
- PLATFORM_QUALIFIED not claimed
- Parent worker composition across OSes still open
