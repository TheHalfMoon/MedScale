# Muse Handoff — MedScale Clinical + Research Intelligence OS

**Status:** `PLANNING_HANDOFF_ONLY`  
**Important:** this file prepares implementation execution. It does not itself promote Spec 075+.

Use ENGLISH ONLY for all Muse conversation, repository content, code, comments, commands, specifications, plans, tasks, evidence, commit messages, PR bodies, reviewer responses, reports and GitHub-facing text.

# PRIMARY DIRECTIVE

Continue MedScale from exact live GitHub/repository truth.

After this planning packet is canonically accepted and the next Research OS unit is promoted, execute the dependency-ordered implementation program autonomously until the currently authorized MedScale program is genuinely complete under canonical governance and real evidence.

Do not interpret this handoff as permission to implement unpromoted Specs 075–092.

Live repository authority always wins.

# FIRST ACTION — REBUILD LIVE TRUTH

Before any mutation, verify:

- default branch;
- exact `origin/main`;
- current worktrees/branches;
- open/draft PRs;
- required checks/rulesets;
- canonical build queue;
- implementation authority;
- current promoted spec;
- exact predecessor closure evidence;
- current Research OS planning packet;
- current UI/V0 lane;
- current Orca/source-planning lane;
- real PHI/release/privacy gates.

Read at minimum:

1. `CURSOR.md`
2. `AGENTS.md`
3. `docs/planning/START_HERE.md`
4. `docs/planning/IMPLEMENTATION_AUTHORITY.md`
5. `docs/planning/BUILD_QUEUE.md`
6. `docs/planning/CLINICAL_RESEARCH_OS_PLAN_INDEX.md`
7. every file in that index's read order
8. existing canonical Research OS V2 contract/acceptance/verification docs
9. the exact active spec package after promotion

Never use remembered SHA/state as current truth.

# PROGRAM PRINCIPLE

MedScale's target is one local-first Clinical + Research Intelligence OS:

- local medical scribe;
- evidence-grounded medical intelligence;
- local medical model fleet;
- patient/clinical graph;
- Data Workbench;
- cohorts and analytics;
- research/literature canvas;
- governed browse;
- documents/audio;
- team collaboration;
- institutional interoperability;
- extensibility.

Do not build separate mini-products with separate truth stores.

# CORE INVARIANTS

Preserve:

- one Rust-owned authority path;
- local-first useful core;
- runtime network default deny unless explicitly admitted;
- no hidden remote inference fallback;
- source / derivation / inference / review / effect separation;
- graph as projection, not clinical truth;
- workspace views as references, not copies;
- evidence close to consequential claims;
- explicit unknown/stale/conflict/partial states;
- heavy/untrusted runtimes isolated;
- durable controlled-effect semantics for external writes;
- no real PHI until live canonical authority says otherwise;
- MESC remains separate.

# IMPLEMENTATION ORDER

Do not jump ahead.

The candidate sequence is:

```text
075 Data Source Fabric + Data Workbench Foundation
076 Collaboration Substrate
077 MedAgent + Evidence Copilot Foundation
078 Model Fleet + Compare
079 Privacy Gate
080 Governed Browse + Medical Literature Acquisition
081 AudioFlow Foundation + Local Scribe MVP
082 Analytics Gate + Cohort Builder
083 Clinical Graph + Knowledge/Research Canvas
084 MedScale Hub
085 MedScale Compute
086 R Workspace
087 Community Extensions
088 AudioFlow Advanced + Clinical Documentation Expansion
089 Research Packs + Evidence Packs
090 Institutional Adapters + Clinical Effect Integration
091 Federation
092 Whole-Platform Qualification
```

Actual promotion order must follow the live dependency graph in `RESEARCH_OS_EXECUTION_ROADMAP.md`, not this textual listing alone.

# PROMOTION RULE

If no next unit is currently promoted:

1. do not start product implementation;
2. refine only the smallest dependency-ready candidate through canonical planning/promotion mechanics;
3. bind planning contracts to exact current repository paths/types;
4. create the spec package with:
   - exact base SHA;
   - scope/non-goals;
   - types/contracts;
   - migrations;
   - threat-model delta;
   - privacy classes;
   - recovery/rollback;
   - tests;
   - performance budgets;
   - evidence paths;
   - exact donor decisions;
5. only implement after repository governance records the promotion/authority.

# EXECUTION LOOP

For each promoted unit:

## 1. Baseline
- fetch live main;
- verify exact predecessor evidence;
- create isolated branch/worktree;
- record baseline SHA.

## 2. Contract freeze
Before implementation, freeze the smallest required:
- IDs/types;
- state machines;
- storage schema;
- Core commands;
- worker/tool contracts;
- receipts/evidence;
- UI integration boundary.

## 3. Small vertical slice
Implement the smallest end-to-end path that proves the authority model.

Avoid broad scaffolding with no executable behavior.

## 4. Failure paths
Implement explicit:
- denied;
- unavailable;
- cancelled;
- timeout;
- partial;
- stale;
- unknown;
- corrupt;
- unsupported-version;
- restart/recovery.

## 5. Qualification
Run all active spec gates plus repository-wide gates required by current governance.

Never convert compile success into runtime qualification.

## 6. Evidence
Bind evidence to the exact candidate revision:
- commands;
- outputs;
- test counts;
- fixtures;
- environment/hardware;
- hashes;
- limitations;
- CI run IDs.

## 7. Review/merge
- reconcile main normally;
- no force-push/rebase of shared history;
- rerun affected gates;
- resolve findings;
- merge only after exact-head requirements pass.

## 8. Post-main
- verify actual merge commit;
- verify post-main CI;
- update queue/evidence/status;
- only then advance.

# SPEC 075 STARTING SHAPE

When 075 is genuinely promoted, begin with:

```text
075-00 Live contract binding
075-01 Local file acquisition
075-02 Database read adapters
075-03 Remote dataset sources
075-04 Data Workbench grid
075-05 Import mapping
075-06 Workbench view contracts
075-07 Transformation foundation
075-08 Recovery/performance
075-09 Qualification
```

Do not expand to analytics, R, scribe or graph implementation inside 075.

# MEDICAL SCRIBE RULE

The minimum local scribe path is:

```text
explicit capture
 -> encrypted local audio
 -> VAD/health
 -> local streaming ASR
 -> local diarization
 -> refined transcript revision
 -> bounded clinical context
 -> local note draft
 -> unsupported/conflict checks
 -> linked evidence
 -> clinician review
 -> approved local artifact
```

External EHR effects wait for their owning adapter/effect spec.

No hidden cloud transcription.

# EVIDENCE COPILOT RULE

Every evidence answer should separate:

- search/retrieval result;
- source/citation existence;
- claim-support relationship;
- evidence quality;
- applicability;
- contradiction;
- uncertainty;
- model-generated synthesis.

A citation badge alone is not enough.

# GRAPH RULE

Clinical/Research Graph:

- derived projection only;
- every edge has source/provenance/authority class;
- inferred edges remain inferred;
- temporal semantics preserved;
- no silent patient identity merge;
- no mandatory external graph server unless measured need proves it.

# DATA WORKBENCH RULE

Data Workbench:

- is not the patient authority DB;
- operates over DataSnapshots and MedScale-owned research tables;
- durable transformations create new versioned snapshots;
- AI proposes transformations/queries;
- deterministic validators/executors perform them after approval;
- remote/live source refresh never rewrites old analytical evidence.

# DONOR RULE

Read:
`RESEARCH_OS_V2_SOURCE_QUALIFICATION_LEDGER_2026-09-19.md`.

Do not copy donor code from product similarity alone.

For every dependency/copy/adaptation record:
- exact repo;
- exact SHA;
- exact source paths;
- governing license;
- notices;
- target path;
- modifications;
- dependency tree;
- security boundary;
- behavior tests;
- update/rollback/exit strategy.

Specific restrictions:

- NocoDB current source: reference only unless separate rights/licensing changes.
- Teable core apps: reference only by default; exact MIT packages separately reviewable.
- AFFiNE: path-sensitive mixed licensing; no wholesale copy.
- Graphify: Apache-2.0 research donor but clinical semantics remain MedScale-owned.
- OpenMed: Apache-2.0 repository but model/data/terminology rights remain separate.
- Grist Community: Apache-2.0 research donor.
- Baserow OSE: MIT-compatible OSE paths only; exclude premium/enterprise/doc-license material.

# UI RULE

V0/Identity V2 remains visual authority.

Do not independently invent a competing UI system.

When production-porting V0 decisions:
- keep semantic tokens;
- preserve light/dark parity;
- keep evidence/provenance states explicit;
- use graphs as optional lenses;
- optimize dense table/data/clinical work for readability;
- never use color alone for clinical/truth state.

# SECURITY RULE

Treat as hostile:
- model output;
- web content;
- imported files;
- extension code;
- model artifacts;
- dataset content;
- external adapter responses.

No hostile input can grant itself capabilities.

# CLINICAL CLAIM RULE

Do not claim:
- clinical correctness;
- medical-device status;
- compliance/certification;
- HIPAA/GDPR/PDPL conformance;
- scribe parity/superiority;
- OpenEvidence/Abridge replacement;
- safe real-PHI use;
- release readiness

without the exact owning evidence and canonical authority.

# STOP CONDITIONS

Do not stop after one slice, PR, spec or milestone if more authorized dependency-ready work exists.

Stop only when:

1. the current authorized program is genuinely closed canonically; or
2. every remaining blocker is external/human-only and there is no independent repository-owned authorized work left.

If blocked:
- record exact blocker/evidence;
- continue independent work;
- never fabricate resolution.

# PROJECT-COMPLETION TEST

Before claiming the project is finished, recompute:

- proven completion criteria;
- partial criteria;
- missing criteria;
- N/A criteria;
- external gates;
- repository-owned residuals;
- release/privacy/multi-client state;
- current whole-platform qualification evidence.

No narrative completion claim outranks live evidence.
